dependencies:
  override:
    - echo "No dependencies"
test:
  override:
    - echo $(pwd)
    - ls
    - echo $GOPATH

#dependencies:
#  cache_directories:
#    - "~/cache/gonative"
#  pre:
#    - "git submodule init"
#    - "git submodule update"
#    - "go get github.com/tools/godep"
#    - "go get github.com/mitchellh/gox"
#    - "./circle/build_gonative.bash"
#  override:
#    - "gox -osarch='windows/amd64 linux/amd64 darwin/amd64' github.com/salsaflow/salsaflow":
#      environment:
#        GOPATH: "$HOME/workspace:$GOPATH"
#        GOROOT: "$HOME/cache/gonative/go"
#        PATH:   "$HOME/cache/gonative/go/bin:$PATH"
#test:
#  override:
#    - "echo 'NO TESTS'":
